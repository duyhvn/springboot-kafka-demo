# Kafka Properties
io.reflectoring.kafka.bootstrap-servers=localhost:9092
io.reflectoring.kafka.topic-1=reflectoring-1
io.reflectoring.kafka.topic-2=reflectoring-2
io.reflectoring.kafka.topic-3=reflectoring-3
io.reflectoring.kafka.topic-4=reflectoring-user
logging.level.org.apache.kafka = WARN

## Spring DATASOURCE (DataSourceAutoConfiguration &amp; DataSourceProperties)
spring.datasource.driver-class-name=com.mysql.jdbc.Driver
spring.datasource.url=jdbc:mysql://mysql-40881-0.cloudclusters.net:18914/mysql_demo
spring.datasource.username=admin
spring.datasource.password=s10AZuGF

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update


elasticsearch.clustername = oin-searchly
elasticsearch.host = localhost
elasticsearch.port = 9300

#
# Home directory of the embedded Elasticsearch instance. Default to the
# current working directory.
#
#spring.data.elasticsearch.properties.path.home=target/elastic-embedded
allowedOrigins.host=localhost:3000,localhost:3001